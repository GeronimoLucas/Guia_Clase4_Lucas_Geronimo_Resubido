#include<stdio.h>
#include<stdlib.h>
int main()
{
int sdo_bas,cant_hjos,adici,categoria,sdo_total;
printf("Ingrese sueldo basico");
scanf("%d",&sdo_bas);
printf("Iingrese su categoria en la empresa");
scanf("%d",&categoria);
printf("Iingrese cantidad de hijos");
scanf("%d",&cant_hjos);
if (categoria=1)
adici=((sdo_bas)+(sdo_total/100*10));
scanf("%d",&categoria);
else
if (categoria=2)
adici=((sdo_bas)+(sdo/100*20));
scanf("%d",&categoria);
else
adici=((sdo_bas)+(0);
}
}
if (cant_hjos=>4)
{
cant_hjos=((cant_hijos*300) +200));
scanf("%d”, &cant_hjos);
else
cant_hjos=(cant_hjos*300);
scanf("%d”, &cant_hjos);
sdo_total=(sdo_bas+adici+cant_hijos);
scanf("%d”, &sdo_total);
sprintf("tu sueldo total es de:\n %d",sdo_total);
return 0;
}
